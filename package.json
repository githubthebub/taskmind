{
  "name": "focus-metric-engine",
  "version": "1.0.0",
  "description": "Offline-first, client-side task accelerator: visuospatial attention puzzle, Yerkes-Dodson behavioral state engine, exhaustion premortem crucible, and quota-gated identity priming. Zero cloud dependencies.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
